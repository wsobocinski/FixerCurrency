<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewmodel"
            type="com.example.fixercurrency.MainFragmentViewModel" />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainFragment">

        <TextView
            android:id="@+id/date"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@{viewmodel.dateString}"
            android:textSize="30sp"
            tools:text="@string/hello_blank_fragment"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@{viewmodel.plnExchangeValue}"
            android:textSize="30sp"
            android:layout_marginTop="30dp"
            tools:text="@string/hello_blank_fragment"/>

        <Button
            android:id="@+id/previous_day_value_button"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_marginTop="100dp"
            android:text="@string/check_previous_day_value"/>

    </FrameLayout>
</layout>